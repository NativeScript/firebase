export const enum NotificationAndroidVisibility {
	VISIBILITY_PRIVATE,
	VISIBILITY_PUBLIC,
	VISIBILITY_SECRET,
}

export const enum NotificationAndroidPriority {
	PRIORITY_DEFAULT,
	PRIORITY_HIGH,
	PRIORITY_LOW,
	PRIORITY_MAX,
	PRIORITY_MIN,
}
